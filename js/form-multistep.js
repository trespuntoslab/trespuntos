(function(){'use strict';var TOTAL_STEPS=5;var stepsNav=document.getElementById('steps-nav');var progressFill=document.getElementById('progress-fill');var stepCounter=document.getElementById('step-counter');var stepPercent=document.getElementById('step-percent');function val(id){var el=document.getElementById(id);return el?el.value.trim():'';}
function isValidEmail(email){return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);}
function showError(id){var el=document.getElementById(id);if(el)el.style.display='flex';}
function hideError(id){var el=document.getElementById(id);if(el)el.style.display='none';}
function hideAllErrors(){document.querySelectorAll('.field-error').forEach(function(el){el.style.display='none';});}
function goToStep(n){document.querySelectorAll('.form-step').forEach(function(step){step.classList.remove('active');});var target=document.getElementById('step-'+n);if(target)target.classList.add('active');if(stepsNav){stepsNav.querySelectorAll('.step-item').forEach(function(item){var stepNum=parseInt(item.getAttribute('data-step'));item.classList.remove('active','done');if(stepNum<n)item.classList.add('done');if(stepNum===n)item.classList.add('active');});}
var percent=Math.round((n/TOTAL_STEPS)*100);if(progressFill)progressFill.style.width=percent+'%';if(stepCounter)stepCounter.textContent='PASO '+n+' DE '+TOTAL_STEPS;if(stepPercent)stepPercent.textContent=percent+'%';var formArea=document.querySelector('.form-area');if(formArea)formArea.scrollTop=0;window.scrollTo(0,0);}
function validateStep(n){hideAllErrors();var valid=true;if(n===1){if(!val('s1-name')){showError('err-name');valid=false;}
if(!val('s1-company')){showError('err-company');valid=false;}
if(!val('s1-email')||!isValidEmail(val('s1-email'))){showError('err-email');valid=false;}
if(!val('s1-phone')){showError('err-phone');valid=false;}}
if(n===2){var projectType=document.querySelector('input[name="project_type"]:checked');if(!projectType){var errType=document.getElementById('err-project-type');if(errType){errType.style.display='flex';}
valid=false;}}
if(n===3){if(!val('s3-situation')||val('s3-situation').length<20){showError('err-situation');valid=false;}
if(!val('s3-target')){showError('err-target');valid=false;}
if(!val('s3-problems')){showError('err-problems');valid=false;}}
return valid;}
window.goNext=function(current){if(!validateStep(current))return;if(current<TOTAL_STEPS){goToStep(current+1);}};window.goPrev=function(current){if(current>1){goToStep(current-1);}};(function(){var allSelects=document.querySelectorAll('.custom-select[id]');allSelects.forEach(function(cs){var trigger=cs.querySelector('.custom-select-trigger');var label=cs.querySelector('.custom-select-value');var options=cs.querySelectorAll('.custom-select-option');if(!trigger)return;trigger.addEventListener('click',function(e){e.preventDefault();e.stopPropagation();allSelects.forEach(function(other){if(other!==cs){other.classList.remove('open');other.setAttribute('aria-expanded','false');}});var isOpen=cs.classList.contains('open');if(isOpen){cs.classList.remove('open');cs.setAttribute('aria-expanded','false');}else{cs.classList.add('open');cs.setAttribute('aria-expanded','true');}});options.forEach(function(opt){opt.addEventListener('click',function(e){e.stopPropagation();options.forEach(function(o){o.classList.remove('selected');});opt.classList.add('selected');if(label)label.textContent=opt.textContent.trim();trigger.classList.add('has-value');cs.value=opt.getAttribute('data-value')||opt.textContent.trim();cs.classList.remove('open');cs.setAttribute('aria-expanded','false');});});});document.addEventListener('click',function(e){allSelects.forEach(function(cs){if(!cs.contains(e.target)){cs.classList.remove('open');cs.setAttribute('aria-expanded','false');}});});})();document.querySelectorAll('.radio-option').forEach(function(label){var input=label.querySelector('input[type="radio"]');if(!input)return;label.addEventListener('click',function(){var groupName=input.getAttribute('name');document.querySelectorAll('.radio-option input[name="'+groupName+'"]').forEach(function(r){r.closest('.radio-option').classList.remove('selected');});label.classList.add('selected');});});document.querySelectorAll('.budget-option').forEach(function(label){var input=label.querySelector('input[type="radio"]');if(!input)return;label.addEventListener('click',function(){var groupName=input.getAttribute('name');document.querySelectorAll('.budget-option input[name="'+groupName+'"]').forEach(function(r){r.closest('.budget-option').classList.remove('selected');});label.classList.add('selected');});});document.querySelectorAll('.check-option').forEach(function(label){var input=label.querySelector('input[type="checkbox"]');if(!input)return;label.addEventListener('click',function(){label.classList.toggle('selected');});});var blockMap={'web-corporativa':'block-web-corporativa','ecommerce':'block-ecommerce','saas':'block-saas','app':'block-app'};document.querySelectorAll('input[name="project_type"]').forEach(function(radio){radio.addEventListener('change',function(){var val=this.value;Object.keys(blockMap).forEach(function(key){var block=document.getElementById(blockMap[key]);if(block)block.style.display='none';});var targetId=blockMap[val];if(targetId){var target=document.getElementById(targetId);if(target)target.style.display='';}});});document.querySelectorAll('input[name="needs_blog"]').forEach(function(radio){radio.addEventListener('change',function(){var wrap=document.getElementById('s3-existing-blog-wrap');if(wrap)wrap.style.display=this.value==='si'?'':'none';});});document.querySelectorAll('input[name="needs_automation"]').forEach(function(radio){radio.addEventListener('change',function(){var wrap=document.getElementById('s4-automation-wrap');if(wrap)wrap.style.display=this.value==='si'?'':'none';});});document.querySelectorAll('input[name="needs_crm"]').forEach(function(radio){radio.addEventListener('change',function(){var wrap=document.getElementById('s3-crm-name-wrap');if(wrap)wrap.style.display=this.value==='si'?'':'none';});});(function(){var MAX_FILES=3;var MAX_SIZE_MB=10;var ALLOWED_TYPES=['application/pdf','application/msword','application/vnd.openxmlformats-officedocument.wordprocessingml.document'];var ALLOWED_EXT=['.pdf','.doc','.docx'];window.__uploadedFiles=[];var zone=document.getElementById('s3-upload-zone');var input=document.getElementById('s3-file');var listEl=document.getElementById('s3-file-list');var errEl=document.getElementById('err-file');if(!zone||!input)return;function formatSize(bytes){if(bytes<1024)return bytes+' B';if(bytes<1048576)return(bytes/1024).toFixed(1)+' KB';return(bytes/1048576).toFixed(1)+' MB';}
function isAllowed(file){var ext='.'+file.name.split('.').pop().toLowerCase();return ALLOWED_EXT.indexOf(ext)!==-1||ALLOWED_TYPES.indexOf(file.type)!==-1;}
function renderList(){if(!listEl)return;listEl.innerHTML='';window.__uploadedFiles.forEach(function(file,i){var item=document.createElement('div');item.className='file-upload-item';item.innerHTML='<div class="file-upload-item-name">'+
'<svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">'+
'<path d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"/>'+
'</svg>'+
'<span>'+file.name+'</span>'+
'</div>'+
'<span class="file-upload-item-size">'+formatSize(file.size)+'</span>'+
'<button type="button" class="file-upload-item-remove" data-index="'+i+'" title="Eliminar">&times;</button>';listEl.appendChild(item);});listEl.querySelectorAll('.file-upload-item-remove').forEach(function(btn){btn.addEventListener('click',function(e){e.stopPropagation();var idx=parseInt(this.getAttribute('data-index'));window.__uploadedFiles.splice(idx,1);renderList();});});}
function addFiles(fileList){if(errEl)errEl.style.display='none';for(var i=0;i<fileList.length;i++){var file=fileList[i];if(window.__uploadedFiles.length>=MAX_FILES){if(errEl){errEl.innerHTML='<svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M12 9v3.75m9-.75a9 9 0 11-18 0 9 9 0 0118 0zm-9 3.75h.008v.008H12v-.008z"/></svg>Máximo '+MAX_FILES+' archivos permitidos.';errEl.style.display='flex';}
break;}
if(!isAllowed(file)){if(errEl){errEl.innerHTML='<svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M12 9v3.75m9-.75a9 9 0 11-18 0 9 9 0 0118 0zm-9 3.75h.008v.008H12v-.008z"/></svg>Archivo no válido. Solo PDF o Word.';errEl.style.display='flex';}
continue;}
if(file.size>MAX_SIZE_MB*1024*1024){if(errEl){errEl.innerHTML='<svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M12 9v3.75m9-.75a9 9 0 11-18 0 9 9 0 0118 0zm-9 3.75h.008v.008H12v-.008z"/></svg>"'+file.name+'" excede el máximo de '+MAX_SIZE_MB+' MB.';errEl.style.display='flex';}
continue;}
var isDuplicate=window.__uploadedFiles.some(function(f){return f.name===file.name&&f.size===file.size;});if(!isDuplicate){window.__uploadedFiles.push(file);}}
renderList();input.value='';}
input.addEventListener('change',function(){addFiles(this.files);});zone.addEventListener('dragover',function(e){e.preventDefault();zone.classList.add('dragover');});zone.addEventListener('dragleave',function(){zone.classList.remove('dragover');});zone.addEventListener('drop',function(e){e.preventDefault();zone.classList.remove('dragover');addFiles(e.dataTransfer.files);});})();goToStep(1);console.info('✅ TRES PUNTOS: Form multi-step inicializado ('+TOTAL_STEPS+' pasos)');})();